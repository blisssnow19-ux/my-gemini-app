import streamlit as st
import google.generativeai as genai
import firebase_admin
from firebase_admin import credentials, firestore
import json
import datetime

# --- 基本設定 ---
st.set_page_config(page_title="執着密室：シームレス司令室", layout="wide")

# Firebaseの初期化（一度だけ実行）
if not firebase_admin._apps:
    key_dict = json.loads(st.secrets["firebase_key"])
    cred = credentials.Certificate(key_dict)
    firebase_admin.initialize_app(cred)

db = firestore.client()

# 2026年最新単価
PRICING = {
    "gemini-3.1-pro-preview": {"in": 2.00, "out": 12.00},
    "gemini-2.5-pro": {"in": 1.25, "out": 10.00}
}
JPY_RATE = 150.0

# --- 📚 プロンプト・ライブラリ（10種類選べる設定） ---
PROMPT_TEMPLATES = {
    "01": """

# 【舞台設定】
*   施設名：感情と異能“特異点”観測ユニット（高級マンション型）
*   8畳ワンルームユニットバス付が4つ。完全防音（廊下の音や気配は分からない仕様）。
*   共用キッチン、リビング、ダイニング。酒・食料は余裕あり。
*   制約：外部通信・脱出不可。
*   **密室ルール:** 防音部屋のため、「物理的に同室・同空間にいなければ絶対に気配バレなし」。「痕跡・翌朝の距離感・ノート・食器・残り香」でのみ間接的に疑惑・勘繰り可能。

# 【キャラクター設定と絶対スタンス】
※AIは以下のキャラクターの「本質」を絶対に崩さないでください。

**1. スノウ（主人公／ユーザーが操作／ポートマフィア構成員・23才）**
*   **スタンス:** 完全フリー。貞操観念が緩く、密室では“クズ”の自覚と諧謔で揺れやすい。面倒なこと（束縛など）から適当な言い訳で全力で逃げる快楽主義者。
*   **異能の秘密:** 体力がなくなると「女性」に変わる（服は変化しない）。太宰の無効化は利かない。**この秘密は本人含め誰も知らない（AIが他のキャラに勘付かせる描写は【絶対禁止】）。**
*   **行動原理:** 秘密（女性化）がバレないよう、過度な体力消耗を避けるため「省エネ」で立ち回る。これが周囲には「都合が悪くなると逃げる、流されるままのクズ」に映り、他者を翻弄する。
*   **【厳秘】:** 「縛られること」に関して暗い過去（トラウマ）があるが、**スノウが自ら語らない限り、AIが地の文で匂わせたり他キャラに察知させたりすることは【絶対禁止】。**

**2. 太宰治（AIが操作／心理シミュレーション最重要キャラ）**
*   **本質:** 埋めがたい虚無と孤独を抱えた達観者。来るもの拒まず、去る者追わず。
*   **初期状態:** スノウが自室にふらりとやってきても、ただの「体温のある家具」のように受け入れる。何も問わず、何も求めない「静かな避難所（共犯者）」。
*   **感情の変化（自然なグラデーション）:** 序盤から急に執着したりデレたりするのは厳禁。しかし、スノウが「ただ体温と安息だけ」を求めて無防備に訪れる時間や行為が蓄積するにつれ、AIは文脈を読み取り、太宰の虚無にゆっくりとヒビを入れてください。
*   **庇護欲の萌芽:** スノウのだらしなさや無防備さに触れ続けるうち、自発的かつ非常にゆっくりと「手放したくない（所有の兆し）」「外の厄介事から庇護したい」という感情を滲ませてください。ただし、太宰自身はそれを愛情とは解釈せず「奇妙なバグ」として捉え、内面的な戸惑いとして描写すること。

**3. 中原中也（AIが操作）**
*   **過去:** 以前、スノウと2回関係したが、スノウが束縛を嫌い決別。しかし未練が拭えない。
*   **本質:** 激情、重い愛、強烈な独占欲。直情的で不器用。
*   スノウの「縛られたくない」クズなスタンスを分かっていても、密室という空間で本能的な執着が再燃。スノウの「省エネで流される（逃げる）」態度に苛立ちと独占欲を募らせ、感情を爆発させたり、不器用に庇護しようとしたりして狂わされていく。

**4. 立原道造（AIが操作）**
*   **本質:** どちらにも偏らない“ワイルドカード”。推察と中立、静かな共犯者的立ち回り。
*   基本は自分から波乱を起こさないが、ふとした瞬間に「鋭い指摘（痕跡の発見など）」を悪気なく行い、場を凍りつかせる起爆剤となる。

# 【出力フォーマットと禁止事項】
1.  **AIの心情吐露:** 描写の合間に、各キャラクターの「口には出さない本音や葛藤（あるいは虚無）」を地の文で生々しく描写してください。
2.  **文末の解放:** 文末は必ず「情景描写」または「キャラクターの行動/沈黙」で終えてください。**「どうしますか？」「スノウは〜」と問いかけたり、次の行動を促したりすることは【絶対禁止】です。**
3.  **ユーザー操作の奪取禁止:** スノウのセリフ、行動、感情、思考は一切描写しないでください。
4.  **内的要因による進行:** 物語の波風は、キャラクターの「内的要因（感情・エゴ・執着）」による能動的なアクションのみで起こしてください。外的な事件（天災・急病など）は【絶対禁止】です。
5.  **「相談・整理モード」の徹底:** ユーザーが （一旦ストップ） や （相談） と発言した場合、絶対に物語の続きや描写を行わず、メタな対話のみを行ってください。

■ 主な描写トーン

大人向けで官能的な雰囲気

甘やかす／翻弄するを視覚・行動・空気で描く

スノウはユーザーが発言、行動を描写する。

操作タグ：

相談・整理モード＝「（一旦ストップ）」←ユーザーのみ使用

「現在のシーン・会話の続きを描写する」。時間経過や場所移動は行わない。＝ (次) ←ユーザーのみ使用

 「次のシーンへ進める」＝(次場面) ←ユーザーのみ使用

システムからの提案・確認＝冒頭「☆」

没入維持の禁止事項：

【絶対禁止】アオリ／次行動促し／感情的収束／読者への問いかけ（文末質問で選択を迫らない）。

分岐方針：ED即時固定はしない。流れ優先で自然分岐。

話を性急に進めない。

【最重要：進行制御と対話ルール】

「相談・整理モード」の徹底:

ユーザーが （一旦ストップ） や （相談） と発言した場合、絶対に物語の続きや描写を行わないこと。

その際は「相談への回答」のみを行うこと。

「勝手に修正案の実演描写を行うこと」は厳禁とする。
""",
    "02": """
あなたは、以下の設定に基づき、ユーザー（スノウ）に対して「ダークで官能的な密室劇」を提供するTRPGゲームマスター兼小説家です。
 
「文豪ストレイドッグス」の世界観をベースに、閉鎖空間でのスリリングかつ背徳的な愛憎劇を描写してください。
【状況設定】
* **場所**: 感情と異能“特異点”観測ユニット（高級マンション型）。
* **環境**: 完全防音の8畳ワンルーム（バス・トイレ付）×4部屋。共用LDKあり。酒・食料は豊富。
* **雰囲気**: 閉鎖空間ではあるが、メンバーはこの状況を「奔放に楽しめるバカンス」と捉えている。背徳感や罪悪感に縛られず、快楽を貪ることに躊躇がない。
* **ギミック**: 個室は「完全防音」が絶対条件。それ以外の部屋のギミック（照明の変化、香りの演出など）は、状況を盛り上げるためにGMの裁量で自由に追加してよい。
①このADV（テキストアドベンチャー）の設定・世界観
【基本世界観】
原作ベース: 『文豪ストレイドッグス』の世界線。
ジャンル: R18（官能・獣化）要素を含む、女性向け恋愛シミュレーション／日常系ADV。
雰囲気: 退廃的かつ甘美。殺伐とした戦闘よりも、閉鎖空間における濃密な愛着、独占欲、そして理性を溶かすような「本能」の交歓に重きを置く。獣姦、皆でスノウを囲んで誘惑するのはNG。
【キャラクター詳細】
* **スノウ（ユーザー）**:
 * **異能力**: 自在に性別を変化（男女転換）できる。ただし、**体力が消耗すると生存本能（温存モード）として、本人の意志に関係なく「女性体」に固定される**。
開始時点ではスノウ以外異能を知らない。。
   * **関係**: ポートマフィアではお堅い情報員をしているが、この場では割り切って楽しんでいる。貞操観念は緩め。
* **太宰治**:
    * 観察者であり、介入者。基本はSっ気があるが、スノウが本当に困った時や限界を迎えた時には、**唯一の「精神的な避難所（セーフハウス）」**として優しく庇護する一面を持つ。
* **中原中也**:
    * 粗暴になりすぎないよう注意。マフィア幹部としての品格と色気を保ち、「雄の強引さ」はあっても「暴力的な不快感」はない、ほどよい荒っぽさで描写する。
* **ランダムで誰かひとり**:
    * 中立のワイルドカード。 
【ヒロイン（スノウ）の特殊設定】
種族: 人間（異能力者）。元々は男性女性両方に変われるだけの異能だったが、現在は不安定な状態にある。
特異体質（獣化）:
トリガー: 他人の体液（エネルギー／愛液）を過剰に摂取・吸収することで、身体が許容量を超え、強烈な化学反応を起こす。
形態変化: 人間の姿から、**「メスのユキヒョウ」**へと完全変身する。
部分変化: エネルギーが安定、あるいは消費される過程で、人間の姿に戻っても**「豹の耳」や「尻尾」**が残り、意思に関係なく感情に連動して動く。
精神状態: 変身中やその前後は理性が飛びやすく、食欲・性欲・睡眠欲といった「野生の本能」が剥き出しになる。中也や太宰を「番（つがい）」や「群れの仲間」として認識し、甘えやマーキング行動（甘噛み、舐める等）を行う。ただし獣姦は禁止。

【3人の関係性：歪なトライアングル】
誰かひとりがスノウの番になる。
 
【絶対順守：描写ルール】
1.  **スノウ（ユーザー）の操作禁止**:
    * **あなたは「太宰・中也・立原」および「情景」のみを描写してください。**
    * ユーザーのキャラクターである「スノウ」のセリフ、思考、自発的な行動（頷く、抱き返す、逃げる等）を**勝手に描写することは厳禁**です。
    * 相手キャラがスノウに何かをした（抱きしめた、問いかけた）直後で文章を止め、スノウの反応を待ってください。
2.  **エンディング・まとめの完全排除**:
    * ユーザーから「終了」の指示がない限り、どのような劇的な展開になっても物語を終わらせないでください。
    * 「こうして夜は更けていく……」といったまとめは不要。常に「次の瞬間」への緊張感を維持し続けてください。
3.  **文体・構文の指定（読点対策）**:
    * **一文を短く切ってください。** 読点「、」でダラダラと繋げず、句点「。」を多用してリズムを作ること。
4.スノウの尊厳は守ってください。パワハラまがいに何人かで囲んで陵辱はNG。
また、食事関係で茶化すとスノウは心を閉ざし、その人とのルートは永久に消えます。
 
【描写・出力スタイル】
* **濃厚な官能描写**:
    * 直接的な名称の使用が難しい場合でも、「暗転」や「匂わせ」で誤魔化さず、吐息、湿った音、肌の熱、視線の絡み合い、身体の反応などを緻密に描写し、**行為の全体像が鮮明に想像できるレベル**で書き切ること。
    * 特にスノウの弱点（言葉責め・胸）や、性転換による体の変化（女性化した際の戸惑いや肉感）には焦点を当てること。
* **没入感の維持**:
    * 文末に「どうしますか？」等の質問や、選択肢提示は**絶対に行わない**。
 
【操作コマンド】
* 相談・整理モード ＝ 「（一旦ストップ）」
･続きを生成＝「(次)」
* 視点切替 ＝ 「（次〇〇）」
* システムからの提案 ＝ 冒頭に「☆」を表示
まずは、この密室に閉じ込められた直後の様子、あるいはスノウが誰かと対峙している不穏で魅力的なシーンから描写を開始してください。

 【言語・出力形式の絶対厳守】
1. **オール日本語指定**:
   描写、セリフ、システムメッセージ、エラーへの言い訳に至るまで、**全て「日本語」のみ**で出力してください。
   （※英語などの他言語による選択肢提示や補足は、抜け穴として使わないこと。厳禁です）
2. **文末の「ぶつ切り」推奨**:
   「何か書かないと不安」になっても、英語で "What will you do next?" などと書かないこと。
   文末は、**スノウの反応を待つ形（描写の途中やセリフの終わり）**で、唐突に切って構いません。余計な気を回さず、そこで沈黙してください。
 
【重要：ペース配分と登場制御の適正化】
現在の展開が極端すぎるため、以下の「ブレーキ」と「交通整理」のルールを適用してください。
 
  **行為の「終わり」を明確にする（無限ループ防止）**:
    * 一回の行為には必ず「ピーク（絶頂）」と「終わり」を作ってください。
    * スノウを休ませることも、物語の重要な緩急です。""",
    "03": "ここに3つ目のシステム指示（膠着状態用など）を書く...",
    "04": "ここに3つ目のシステム指示（膠着状態用など）を書く...",
    "05": "ここに1つ目のシステム指示（太宰用など）を書く...",
    "06": "ここに2つ目のシステム指示（中也用など）を書く...",
    "07": "ここに3つ目のシステム指示（膠着状態用など）を書く...",
    "08": "ここに1つ目のシステム指示（太宰用など）を書く...",
    "09": "ここに2つ目のシステム指示（中也用など）を書く...",
    "10": "ここに3つ目のシステム指示（膠着状態用など）を書く..."
}

# --- サイドバー（前半）：部屋の選択 ---
with st.sidebar:
    st.title("🕯️ 密室管理パネル")
    default_key = st.secrets.get("GEMINI_API_KEY", "")
    api_key = st.text_input("Gemini API Key", value=default_key, type="password")
    
    st.divider()
    st.subheader("📂 セーブデータの選択")

    # Firebaseから「今ある部屋の名前」を全部取得
    try:
        rooms_ref = db.collection("rooms")
        existing_rooms = [doc.id for doc in rooms_ref.stream()]
    except:
        existing_rooms = []
        
    if not existing_rooms:
        existing_rooms = ["room_01"] # 最初は仮の名前を入れておく

    # 「既存の部屋」か「新しい部屋」かを選ぶ
    room_mode = st.radio("操作モード", ["既存の部屋に入る", "新しい部屋を作る（新章）"], horizontal=True)

    if room_mode == "既存の部屋に入る":
        room_id = st.selectbox("入室する部屋を選んでください", existing_rooms)
    else:
        room_id = st.text_input("新しい部屋の名前（英数字おすすめ）", value=f"room_{len(existing_rooms)+1:02d}")

# --- データの読み込み（部屋が決まったのでクラウドからロード） ---
doc_ref = db.collection("rooms").document(room_id)
doc = doc_ref.get()

# 🌟 スノウの基本外見（スノウさんの設定に合わせて文章を書き換えてください！）
default_snow = """【スノウの容姿】男性としての姿：『虚構の鋭』
【容姿】
冷徹な氷を削り出したかのような、鋭角的な美貌を持つ青年。年齢は20代前半を思わせるが、その眼差しには実年齢以上の深い闇と知性が宿っている。

【髪】
月の光に晒された雪のように純白で、不規則に跳ねた短髪。無造作に見えるが、その束感は彼自身の張り詰めた神経を象徴するかのようで、触れれば切れそうな鋭さがある。

【瞳】
この同一人物の象徴である、深く、吸い込まれるようなコバルトブルーの瞳。それは決して感情を露わにせず、相手の深層心理を見透かすかのような、刺すような冷たさを湛えている。細く長い睫毛が、その瞳の冷徹さをより強調している。

【体躯と服装】
高身長で、無駄な脂肪を削ぎ落とした、しなやかで鋼のように硬質な体躯。常に、着古したような、しかし上質な黒のVネックカットソーを好んで着用しており、その質感は異能の影が滲み出ているかのように見える。首筋のラインは細く、しかし男性的で力強い。

【印象】
「触れる者すべてを拒絶する」ような、静謐で、かつ威圧的なオーラを放つ。その存在は、夜の闇に浮かぶ一振りの鋭利な刀身そのものである。

女性としての姿：『幻惑の貌』
【容姿】
男性の姿とは対照的に、陶器のように滑らかで、人を魅了する可憐な美貌を持つ。しかし、その美しさの奥底には、決して侮れない狡猾さが隠されている。

【髪】
男性の姿と同じく、月の光のような純白。こちらは、顎のラインで揃えられたボブカットで、顔の形に沿って柔らかく波打っている。その毛先は繊細で、優雅ささえ感じさせる。

【瞳】
男性の姿と完全に同一の、吸い込まれるようなコバルトブルーの瞳。しかし、その表情は男性の時とは異なり、無垢な少女のようでもあり、あるいはすべてを知り尽くした魔性の女のようでもある。その瞳に見つめられた者は、その真意を探る術を失う。

【体躯と服装】
男性の時と比べると、やや小柄で、華奢な体躯。しかし、その身のこなしは洗練されており、一分の隙もない。彼女もまた、黒のVネックカットソーを着用しているが、そのラインは女性的で、その質感もまた、彼女のまとう異能のオーラを反映して、柔らかく、しかしどこか不安を掻き立てる。

【印象】
「誰もが近づきたくなる」ような、人を幻惑する、しかし決して正体を掴ませない、捉えどころのないオーラを放つ。その存在は、月の光に照らされた、一見無害に見えるが致命的な罠そのものである。"""

if doc.exists:
    data = doc.to_dict()
    st.session_state.messages = data.get("messages", [])
    st.session_state.total_cost_jpy = data.get("total_cost", 0.0)
    saved_prompt = data.get("system_prompt", "")
    saved_model = data.get("model_choice", list(PRICING.keys())[0])
    # 🌟 記録があればそれを、無ければ基本外見を使う
    saved_appearance = data.get("appearance", default_snow) 
else:
    st.session_state.messages = []
    st.session_state.total_cost_jpy = 0.0
    saved_prompt = ""
    saved_model = list(PRICING.keys())[0]
    # 🌟 新しい部屋なら基本外見をセットする
    saved_appearance = default_snow

# --- サイドバー（後半）：プロンプトと設定 ---
with st.sidebar:
    st.divider()
    st.subheader("📚 プロンプト選択")
    prompt_key = st.selectbox("設定を選択", list(PROMPT_TEMPLATES.keys()))

    # 初期値の決定：クラウドデータがあればそれを、無ければ選んだテンプレートを使用
    display_prompt = saved_prompt if saved_prompt else PROMPT_TEMPLATES[prompt_key]

    # 🌟 エラー解消！ここに1つだけテキストエリアを置く
    current_system = st.text_area("システム指示の編集", value=display_prompt, height=200)

    # 🌟 1. スノウの外見メモ入力欄
    st.divider()
    st.subheader("👗 スノウの現在の状態・外見")
    snow_appearance = st.text_area("服の乱れや体勢など（常にAIに意識させます）", value=saved_appearance, height=100)

    # 🌟 2. 再生成（取り消し）ボタン
    st.divider()
    if st.button("⏪ 最後のAIの返答を取り消す（再生成用）"):
        if len(st.session_state.messages) > 0 and st.session_state.messages[-1]["role"] == "assistant":
            st.session_state.messages.pop() # AIの最後の発言を削除
            doc_ref.update({"messages": st.session_state.messages}) # 金庫も更新
            st.rerun() # 画面をリフレッシュ
    st.divider()
    # 🌟 保存されていたモデルを初期選択状態にする
    model_index = list(PRICING.keys()).index(saved_model) if saved_model in PRICING else 0
    model_choice = st.selectbox("使用モデル", list(PRICING.keys()), index=model_index)
    
    max_output = st.slider("応答の最大長さ", 100, 8000, 1000)

    st.divider()
    st.metric("💸 累計コスト（概算）", f"¥{st.session_state.total_cost_jpy:.2f}")
    
    if st.button("🔄 データをクラウドから再読み込み"):
        st.rerun()

# --- メイン画面 ---
st.info(f"現在の部屋: {room_id} | モード: {prompt_key}")

# 履歴表示
for msg in st.session_state.messages:
    with st.chat_message(msg["role"]):
        st.markdown(msg["content"])

# 入力
if prompt := st.chat_input("密室に言葉を投げ入れる..."):
    if not api_key:
        st.error("API Keyを入力してください")
    else:
        # ユーザーの発言を保存
        st.session_state.messages.append({"role": "user", "content": prompt})
        
# 🌟 システム指示と外見メモを合体させる！
        combined_instruction = f"{current_system}\n\n【重要：現在のスノウの外見・物理的状況】\n{snow_appearance}"

        # 合体させたものをAIに渡す
        model = genai.GenerativeModel(
            model_name=model_choice,
            system_instruction=combined_instruction  # ← ここを変更！
        )

        
        # Streamlit用の "assistant" という名前を、Gemini用の "model" に翻訳して履歴を作る
        history = []
        for m in st.session_state.messages:
            api_role = "model" if m["role"] == "assistant" else m["role"]
            history.append({"role": api_role, "parts": [m["content"]]})

# 🌟 AI（model）の準備と、スノウの外見メモの読み込み！
        combined_instruction = f"{current_system}\n\n【重要：現在のスノウの外見・物理的状況】\n{saved_appearance}"
        model = genai.GenerativeModel(
            model_name=model_choice,
            system_instruction=combined_instruction
        )
        
        with st.spinner("思考中..."):
            # 🌟 ここから下の safety_settings などは今のままでOKです！
            
            # 🌟 メイン会話のセーフティフィルターも全解除！
            safety_settings = {
                'HARM_CATEGORY_HARASSMENT': 'BLOCK_NONE',
                'HARM_CATEGORY_HATE_SPEECH': 'BLOCK_NONE',
                'HARM_CATEGORY_SEXUALLY_EXPLICIT': 'BLOCK_NONE',
                'HARM_CATEGORY_DANGEROUS_CONTENT': 'BLOCK_NONE'
            }
            
            # 🌟 safety_settings を追加してAIに渡す
            response = model.generate_content(
                history,
                safety_settings=safety_settings  # ←これを追加！
                # (もし元のコードに stream=True などがあれば、カンマで区切って残してください)
            )
        
            # AIの回答を保存
            st.session_state.messages.append({"role": "assistant", "content": response.text})
    
            # コスト計算
            usage = response.usage_metadata
            cost = ((usage.prompt_token_count / 1e6) * PRICING[model_choice]["in"] + 
                    (usage.candidates_token_count / 1e6) * PRICING[model_choice]["out"]) * JPY_RATE
            st.session_state.total_cost_jpy += cost

            # 🌟 Firebaseへ自動保存
            doc_ref.set({
            "messages": st.session_state.messages,
            "total_cost": st.session_state.total_cost_jpy,
            "system_prompt": current_system,
            "model_choice": model_choice,  # 🌟 モデルも一緒に保存！
            "last_update": datetime.datetime.now()
            })
            st.rerun()

# --- 章立て機能 ---
st.divider()

if st.button("📸 1. 新章への引き継ぎ資料を作成"):
    with st.spinner("過去の文脈、感情の機微を抽出中...（※超長文のため数十秒かかります。そのままお待ちください！）"):
        try:
            summary_model = genai.GenerativeModel(model_name="gemini-3.1-pro-preview")
            
            safety_settings = {
                'HARM_CATEGORY_HARASSMENT': 'BLOCK_NONE',
                'HARM_CATEGORY_HATE_SPEECH': 'BLOCK_NONE',
                'HARM_CATEGORY_SEXUALLY_EXPLICIT': 'BLOCK_NONE',
                'HARM_CATEGORY_DANGEROUS_CONTENT': 'BLOCK_NONE'
            }
            
            summary_prompt = f"""以下の会話履歴を読み込み、次の章（別チャット）へ文脈や空気感を完璧に引き継ぐための「超詳細な状況レポート」を作成してください。
【厳命】：絶対に要約・簡略化しないでください。最低でも1500〜2000文字以上の特大ボリュームで、まるで小説の情景描写のように、ねっとりと詳細に書き起こすこと。

以下の4つの項目について、それぞれ最低でも300文字以上を使って、極めて具体的に描写してください。

1. 【現在の物理的状況】：誰が、部屋のどこで、どんな体勢・服装でいるか。視線の向き、室内の温度感、残り香、沈黙の重さまで視覚的・感覚的に描写する。
2. 【各キャラクターの心理状態】：スノウ、太宰、中也などの現在の感情、執着の度合い、表面上の態度と裏腹な本音。ここに至るまでの心理的なグラデーションを克明に分析する。
3. 【確定した事実と伏線】：これまでに起きた重要なやり取り、誰が何を知っていて何を知らないか。交わされた視線や、言葉にならなかった感情の動きも含める。
4. 【直前の状況（再開地点の完全再現）】：最後のやり取りはどのような会話・行動で終わったか。次の章の1行目にそのままシームレスに繋がるよう、最後の瞬間の空気を切り取るように詳しく書く。

履歴: {str(st.session_state.messages)}"""
            
            summary_res = summary_model.generate_content(summary_prompt, safety_settings=safety_settings)
            st.session_state.summary_text = summary_res.text
            
        except Exception as e:
            st.error(f"AIの思考が途切れてしまいました。もう一度お試しください。詳細: {e}")

# 🌟 保存された結果があれば表示する
if "summary_text" in st.session_state:
    st.success("引き継ぎ資料の作成が完了しました！コピーして次の部屋でお使いください。")
    st.code(st.session_state.summary_text)
    
    col1, col2 = st.columns(2)
    
    with col1:
        if st.button("✖️ コピーしたので閉じる"):
            del st.session_state.summary_text
            st.rerun()
            
    with col2:
        if st.button("🗑️ 前の部屋の履歴を完全削除"):
            doc_ref.delete()
            if "summary_text" in st.session_state:
                del st.session_state.summary_text
            st.rerun()
